services:
  redis:
    image: redis:latest
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    environment:
      - REDIS_ARGS=--requirepass password
    volumes:
      - redis-data:/data

volumes:
  redis-data:
    driver: local
